# URL de l’API pour le frontend (NEXT_PUBLIC_ = injectée au build dans le client)
# En local : http://localhost:3000
# Avec games2.iotactile.com : https://api.games2.iotactile.com (puis rebuild : docker compose -f docker-compose.prod.yml build --no-cache app)
NEXT_PUBLIC_API_URL=http://localhost:3000