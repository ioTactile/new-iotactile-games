# Copier en .env à la racine : cp .env.example .env
# Obligatoire en production : COOKIE_SECRET, JWT_SECRET (générer des valeurs fortes)

# --- Base de données (réseau interne Docker) ---
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin
POSTGRES_DB=games2

# --- API (CORS et URL publiques : telles que vues par le navigateur) ---
# En local : http://localhost:3000 et http://localhost:3001
# Avec ton domaine (ex. jouer en ligne sur games2.iotactile.com) : utiliser https et le même domaine
# BASE_URL_API=https://api.games2.iotactile.com
# BASE_URL_APP=https://games2.iotactile.com
# NEXT_PUBLIC_API_URL=https://api.games2.iotactile.com
BASE_URL_API=http://localhost:3000
BASE_URL_APP=http://localhost:3001

# URL de l’API pour le frontend (NEXT_PUBLIC_ = injectée au build dans le client)
# En local : http://localhost:3000
# Avec games2.iotactile.com : https://api.games2.iotactile.com (puis rebuild : docker compose build --no-cache app)
NEXT_PUBLIC_API_URL=http://localhost:3000

# --- Secrets (à changer en production) ---
COOKIE_SECRET=admin
JWT_SECRET=admin
COOKIE_MAX_AGE=604800
JWT_ACCESS_TTL_SECONDS=900
JWT_REFRESH_TTL_SECONDS=604800
